spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect
  jwt:
    secret: "zvMj1Y868pAWv36FrInMFs4Gq2rkj9XJ"
    redis:
      host: localhost
      port: 0000
  springdoc:
    show-login-endpoint: true
  mail:
    smtp:
      host: smtp.gmail.com
      port: 000
      address: emailAddress@address.com
      personal: personal
      username: emailAddress@address.com
      password: pasword
      properties:
        mail:
          smtp:
            auth: true
            starttls:
              enable: true
              required: true
  app:
    base-url: http://localhost:8080